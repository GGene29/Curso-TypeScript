// Las clases se tienden a iniciar con mayúscula, tanto en la declaración como en el archivo
// Se agrega export, para que pueda usarse fuera del archivo
export class Hero {
    constructor(
        name:string,
        powerId: number,
        age : number
    ){}

}