// Esto es un ejemplo de nameSpace

(()=>{})()

// Los nameSpace esta en una capa por debajo, así que cada vez se usa menos.
// Pero básicamente es una manera de agrupar
// Comparación con módulos
// Los namespaces y los módulos son mecanismos para organizar el código en TypeScript. La principal diferencia es que:

// Namespaces: Son una forma más simple de organizar el código y no tienen un mecanismo de exportación e importación explícito como los módulos. Son más adecuados para proyectos más pequeños o para organizar código dentro de un mismo archivo.
// Módulos: Son más flexibles y permiten una mejor separación de código en diferentes archivos. Son ideales para proyectos grandes y complejos.